app:
  trading_mode: paper
  timezone: Europe/Berlin

trading:
  product_id: BTC-EUR
  start_capital_eur: 50.0

risk:
  max_trade_eur: 10.0
  stop_loss_pct: 1.5
  take_profit_pct: 3.0

execution:
  fee_pct: 0.6
  slippage_pct: 0.10

strategy:
  ema_fast: 20
  ema_slow: 50
  rsi_period: 14
  rsi_entry: 50
  rsi_exit: 45

data:
  candle_granularity: 3600
  candle_limit: 300

backtest:
  period: 1M
  interval: 1h

bot:
  poll_interval_seconds: 60

storage:
  database_path: data/bot.db

dashboard:
  host: 0.0.0.0
  port: 8501
